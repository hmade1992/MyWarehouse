# نظام إدارة مخازن النواقية 📦

نظام شامل لإدارة المخازن والمبيعات مع دعم قراءة فواتير PDF تلقائياً.

## المميزات الرئيسية 🚀

### 📦 إدارة المخزن
- عرض جميع الأصناف المتاحة في المخزن
- تسجيل السحب اليومي يدوياً
- تتبع الكميات المتبقية لكل صنف

### 📅 تقارير يومية
- تقسيم الجرد حسب أيام الأسبوع (السبت، الأحد، الإثنين، إلخ)
- عرض إحصائيات مفصلة لكل يوم
- تحميل تقارير Excel للجرد والمبيعات

### 📊 إحصائيات متقدمة
- تحليل المبيعات حسب الأصناف
- عرض الأصناف الأكثر طلباً
- مخططات بيانية تفاعلية

### 📄 قراءة فواتير PDF
- رفع عدة فواتير PDF مرة واحدة
- استخراج تلقائي للمنتجات والكميات
- مطابقة المنتجات مع المخزن تلقائياً (2000+ صنف)
- خصم الكميات من المخزن بضغطة واحدة
- تسجيل العمليات مع ملاحظة "تم الإدخال عبر فاتورة PDF"

## التثبيت والتشغيل 🛠️

### المتطلبات
- Python 3.8 أو أحدث

### خطوات التثبيت

1. استنساخ المستودع:
```bash
git clone https://github.com/yourusername/MyWarehouse.git
cd MyWarehouse
```

2. إنشاء بيئة افتراضية (اختياري):
```bash
python -m venv venv
# على Windows
venv\Scripts\activate
# على Linux/Mac
source venv/bin/activate
```

3. تثبيت المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```

4. تشغيل التطبيق:
```bash
streamlit run app.py
```

5. فتح المتصفح:
سيتم فتح التطبيق تلقائياً على `http://localhost:8501`

## الاستخدام 📖

### إعداد المخزن الأولي
1. انتقل إلى صفحة "⚙️ الإعدادات والرفع"
2. ارفع ملف Excel يحتوي على عمودين: (الصنف، الكمية)
3. سيتم تحديث المخزن تلقائياً

### تسجيل المبيعات يدوياً
1. في صفحة "🏠 لوحة التحكم والجرد"
2. اختر الصنف من القائمة
3. أدخل الكمية المباعة
4. اضغط "تسجيل السحب"

### قراءة فواتير PDF
1. انتقل إلى صفحة "📄 قراءة فواتير PDF"
2. ارفع ملف أو عدة ملفات PDF
3. اضغط "🔍 استخراج البيانات من الفواتير"
4. راجع المنتجات المستخرجة والمطابقة
5. اضغط "✅ تأكيد الخصم من المخزن"

### عرض التقارير
- **تقارير يومية**: من صفحة "🏠 لوحة التحكم والجرد" → اختر يوم الأسبوع
- **إحصائيات**: من صفحة "📊 صفحة الإحصائيات المتقدمة"
- **تحميل التقارير**: استخدم أزرار التحميل لعمل نسخة Excel

## هيكل المشروع 📁

```
MyWarehouse/
│
├── app.py                 # التطبيق الرئيسي
├── inventory.csv          # ملف المخزن (يُنشأ تلقائياً)
├── sales.csv             # ملف المبيعات (يُنشأ تلقائياً)
├── requirements.txt      # المكتبات المطلوبة
├── .gitignore           # ملفات Git المتجاهلة
└── README.md            # هذا الملف
```

## المكتبات المستخدمة 📚

- **Streamlit**: واجهة المستخدم
- **Pandas**: معالجة البيانات
- **Plotly**: المخططات البيانية
- **OpenPyXL**: التعامل مع ملفات Excel
- **PDFPlumber**: قراءة ملفات PDF

## ملاحظات مهمة ⚠️

1. **ملفات البيانات**: ملفات `inventory.csv` و `sales.csv` لا يتم رفعها على GitHub (مضاف في .gitignore) لحماية البيانات. تأكد من عمل نسخة احتياطية محلية.

2. **أمان البيانات**: لا ترفع ملفات البيانات الحساسة على GitHub أو أي مستودع عام.

3. **دعم PDF**: النظام يدعم استخراج البيانات من فواتير PDF بشرط أن تحتوي على أعمدة "المنتج" و "الكمية".

## المساهمة 🤝

نرحب بجميع المساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء branch للميزة الجديدة
3. إرسال Pull Request

## الترخيص 📄

هذا المشروع متاح للاستخدام الشخصي والتجاري.

## الدعم 💬

لأي استفسارات أو مشاكل، يرجى فتح Issue على GitHub.

---

صُنع بـ ❤️ لإدارة مخازن أفضل

